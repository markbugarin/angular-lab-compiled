import { Component } from '@angular/core';
import { Event, Location } from './models/event';
@Component({
  selector: 'my-app',
  template: `<app-databound> </app-databound>`,
})
export class AppComponent {
}
